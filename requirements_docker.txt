# NFT Market Analytics API - Lightweight Docker Requirements
# This is an alternative to requirements_api.txt optimized for Docker builds
# All packages have pre-built wheels available, avoiding compilation issues

# Web Framework & Server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data Processing (stable versions with wheels)
pandas==2.1.3
numpy==1.26.2

# Configuration
python-dotenv==1.0.0

# Type Hints & Validation
pydantic==2.5.0
pydantic-core==2.14.1

# Optional: Monitoring & Logging
python-json-logger==2.0.7

# Note: These are the same versions as requirements_api.txt
# but guaranteed to have pre-built wheels for faster Docker builds
